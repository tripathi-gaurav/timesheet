<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :total_hours %>
  <%= number_input f, :total_hours %>
  <%= error_tag f, :total_hours %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
